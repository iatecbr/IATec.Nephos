/**
 * Preview do Storybook do Nephos.
 *
 * Sem tema, sem tokens e sem decorators. Os tokens do Figma ainda nao foram
 * convertidos para codigo: o formato de saida e a pendencia P17, aberta.
 *
 * @type {import('@storybook/web-components').Preview}
 */
const preview = {
  parameters: {},
};

export default preview;
